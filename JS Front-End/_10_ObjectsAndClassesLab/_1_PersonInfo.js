function solve(first, second, years) {
    
    const object = {
        firstName: first,
        lastName: second,
        age: years
    }

    return object;
}

console.log(solve('Ivan', 'Ivanov', '20'));